{
  "name": "minisearch-wasm-build",
  "version": "0.4.0",
  "private": true,
  "description": "Build orchestration for the minisearch-wasm npm package (not published; the publishable artifact is pkg/).",
  "type": "module",
  "scripts": {
    "build": "wasm-pack build --target web --release && node scripts/finalize-pkg.mjs",
    "publish:pkg": "npm run build && npm publish ./pkg"
  }
}
